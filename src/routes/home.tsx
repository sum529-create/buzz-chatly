import styled from "styled-components";

const MainWrapper = styled.div`
  flex: 1;
  position: relative;
`;

export default function Home() {
  return (
    <MainWrapper>
      <h1>HOME</h1>
    </MainWrapper>
  );
}
